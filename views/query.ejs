<!DOCTYPE html>
<html>
<head>
	<title>Sakila</title>
	<link rel="stylesheet" type="text/css" href="/styles/tarea.css">

</head>
<body>
<script type="text/javascript">
  function ver (){
  if (<%- id %> == 1) {document.getElementById("t01").style.visibility = "visible ";}else if (<%- id %> == 2) {document.getElementById("t02").style.visibility = "visible ";}else if (<%- id %> == 3) {document.getElementById("t03").style.visibility = "visible ";} else if (<%- id %> == 4) {document.getElementById("t04").style.visibility = "visible "; else if (<%- id %> == 6) {document.getElementById("t06").style.visibility = "visible ";}
  
  
  
  


}
  </script>

    <div align="center"> 
    <button style="margin:20px;" class="btn btn-lg btn-query" onclick="ver()"><span class="glyphicon glyphicon-play"></span> Query # <%= id %></button>   
        <h1> ENUNCIADO: <%= id %></h1>
        <% if (id==1)  { %><p>Peliculas de 3 actores específicos, de 2 categorías específicas, con cantidad de ciudades en las que se mostraran, su descripción y si son en ingles (true /false)</p>
        

            <table style="width: 75%;visibility: hidden;" id="t01" class="table" style="width: 70%; margin-top: 60px;" align="center" >
      <thead>
        <tr>
          <th scope="col">Pelicula</th>
          <th scope="col">Nombre Actor</th>
          <th scope="col">Categoría</th>
          <th scope="col">Inglés</th>
          <th scope="col">Descripción</th>
          <th scope="col">Ciudades</th>
        </tr>
      </thead>
      <tbody>
      <% tabla.forEach(function(tbl)  { %>
        <tr>
          <th scope="row"><%= tbl.Pelicula %></th>
          <td scope="row"><%= tbl.NombreActor %></td>
          <td scope="row"><%= tbl.Categoria %></td>
          <td scope="row"><%= tbl.Ingles %></td>
          <td scope="row"><%= tbl.Descripcion %></td>
          <td scope="row"><%= tbl.Ciudades %></td>
        </tr>
      <% }); %>
      </tbody>
    </table>
        <% } else if (id==2) { %><p>Quien de los empleados rento más películas, la cantidad, y el total de ganancia generada por ese empleado</p>
            <table style="width: 75%;visibility: hidden;" id="t02" class="table" style="width: 70%; margin-top: 60px;" align="center" >
      <thead>
        <tr>
          <th scope="col">Empleado</th>
          <th scope="col">Ventas Totales</th>
          <th scope="col">Ganancia Total</th>
        </tr>
      </thead>
      <tbody>
      <% tabla.forEach(function(tbl)  { %>
        <tr>
          <th scope="row"><%= tbl.Empleado %></th>
          <td scope="row"><%= tbl.RentasTotales %></td>
          <td scope="row"><%= tbl.GananciaTotal %></td>
        </tr>
      <% }); %>
      </tbody>
    </table>
        <% } else if (id==3) { %><p>Lista de películas que nunca han sido rentadas</p>
            <table style=" width: 75%; visibility: hidden;" id="t03" class="table" style="width: 70%; margin-top: 60px;" align="center" >
      <thead>
        <tr>
          <th scope="col">Peliculas Nunca Rentadas</th>

        </tr>
      </thead>
      <tbody>
      <% tabla.forEach(function(tbl)  { %>
        <tr>
          <th scope="row"><%= tbl.PeliculasNuncaRentadas %></th>
        </tr>
      <% }); %>
      </tbody>
    </table>
        <% } else if (id==4) { %><p>Listado de clientes con más de 3 días de retraso en la devolución del película</p>
            <table style="width: 75%;visibility: hidden;" id="t04" class="table" style="width: 70%; margin-top: 60px;" align="center" >
      <thead>
        <tr>
          <th scope="col">Cliente</th>
        </tr>
      </thead>
      <tbody>
      <% tabla.forEach(function(tbl)  { %>
        <tr>
          <th scope="row"><%= tbl.Cliente %></th>
        </tr>
      <% }); %>
      </tbody>
    </table>
        <% } else if (id==5) { %><p>Lista de clientes VIP, entendiendo que los VIP son aquellos clientes han rentado películas semanalmente durante al menos 3 meses.</p> <% } %>
         
        <% } else if (id==6) { %><p>Los 3 productos más vendidos por carrera</p>
            <table style="width: 75%;visibility: hidden;" id="t06" class="table" style="width: 70%; margin-top: 60px;" align="center" >
      <thead>
        <tr>
          <th scope="col">Producto</th>
          <th scope="col">Carrera</th>
          <th scope="col">Cantidad</th>
        </tr>
      </thead>
      <tbody>
      
      <% tabla.forEach(function(tbl)  { %>
      		<% tbl.forEach(function(carrera)  { %>
        <tr>
          <th scope="row"><%= carrera.Nombre %></th>
          <th scope="row"><%= carrera.nombreCarrera %></th>
          <th scope="row"><%= carrera.Cantidad %></th>
        </tr>
        <% });%>
      <% }); %>
      </tbody>
    </table>
        
    </div>

    <div id="showData" class="table-responsive"></div>


</body>
</html>